<Window x:Class="OrderTracker.AddNewOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OrderTracker" 
        xmlns:system="clr-namespace:System;assembly=netstandard"
        mc:Ignorable="d"
        Title="AddNewOrderWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Create New Order" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Customer:" Width="100" VerticalAlignment="Center"/>
                <ComboBox Name="customersComboBox" Width="200"
                          DisplayMemberPath="CompanyName"
                          SelectedValuePath="CustomerID" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Order Date:" Width="100" VerticalAlignment="Center"/>
                <DatePicker Name="orderDataDatePicker" SelectedDate="{x:Static system:DateTime.Now}" Width="200" />
            </StackPanel>

            <TextBlock Text="Products:" FontWeight="Bold" Margin="0,10,0,5" />
        </StackPanel>

        <DataGrid Name="itemsDataGrid" Grid.Row="2" AutoGenerateColumns="False" Height="200">
            <DataGrid.Columns>
                <DataGridComboBoxColumn Header="Product" SelectedValueBinding="{Binding ProductID}"
                                        DisplayMemberPath="ProductName" SelectedValuePath="ProductID" x:Name="ProductList">
                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding DataContext.products, RelativeSource={RelativeSource AncestorType=Window}}" />
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>
                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding DataContext.products, RelativeSource={RelativeSource AncestorType=Window}}" />
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                </DataGridComboBoxColumn>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*" />
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice}" Width="*" />
                <DataGridTextColumn Header="Discount" Binding="{Binding Discount}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Add Row" Click="btnAddRow_Click" Width="100" Margin="5"/>
            <Button Content="Submit Order" Click="btnSubmit_Click" Width="120" Margin="5"/>
            <Button Content="Cancel" Click="btnCancel_Click" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
